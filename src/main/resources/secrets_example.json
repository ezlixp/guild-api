{
  "guild_raid_urls": {
    "(guild prefix)": "(guild api base url with / at the end)"
  },
  "validation_key": "(validation key for your guild's token server. note that the endpoint for the token server must be 'baseurl + /auth/getToken')"
}